package com.example.stadiumlocator.ui.theme

import androidx.compose.ui.graphics.Color

// Colors for Light Theme
val LightBackground = Color(0xFFF5F5F5) // Un grigio chiaro per lo sfondo
val LightSurface = Color(0xFFFFFFFF)    // Bianco per le superfici

// Colors for Dark Theme
val DarkBackground = Color(0xFF121212)  // Un grigio scuro per lo sfondo
val DarkSurface = Color(0xFF1E1E1E)    // Grigio scuro per le superfici

// Colors for Text
val TextColorLight = Color(0xFF000000) // Nero per il testo in tema chiaro
val TextColorDark = Color(0xFFFFFFFF)  // Bianco per il testo in tema scuro

// Accent Colors
val AccentColorLight = Color(0xFF4CAF50) // Verde accento per il tema chiaro
val AccentColorDark = Color(0xFF66BB6A)  // Verde chiaro per il tema scuro

// Existing Colors
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
